---
title: 编程资源
tags: [MOC]
---

# 编程资源

> 自动聚合所有编程相关笔记。

## 开源项目

```dataview
TABLE date AS "日期", source AS "来源"
FROM "编程"
WHERE contains(tags, "编程/开源项目")
SORT date DESC
```

## 前端

```dataview
TABLE date AS "日期", source AS "来源"
FROM "编程"
WHERE contains(tags, "编程/前端")
SORT date DESC
```

## Python

```dataview
TABLE date AS "日期", source AS "来源"
FROM "编程"
WHERE contains(tags, "编程/Python")
SORT date DESC
```

## 后端 / API / 架构

```dataview
TABLE date AS "日期", source AS "来源"
FROM "编程"
WHERE contains(tags, "编程/后端") OR contains(tags, "编程/API") OR contains(tags, "编程/架构")
SORT date DESC
```
